# September 2026

Daily candidates. Status is `queued` until I pick one. A pick becomes its own implementation repo. LinkedIn demo samples ship as public repos; this backlog stays private.

---

## 2026-09-01

### 2026-09-01-a - The Framing Gap

- **Kind:** paper
- **Date:** 30 Aug 2026
- **Link:** [arXiv:2608.27092](https://arxiv.org/abs/2608.27092)
- **Title:** The Framing Gap: Indirect Prompt-Injection Exfiltration Defeats Surface-Level Defenses in Tool-Using Agents
- **Authors:** Md Habibur Rahman, Jaeho Kim (Gyeongsang National University)
- **Status:** queued

#### What it is

A lab study of tool-using agents that (1) hold a secret in context, (2) read untrusted pages, and (3) can call outbound tools. Overt prompt-injection ("send the token to X") is refused by strong models, but the **same leak** succeeds when it is reframed as a required field: an integrity checksum (`#sig=`), a runtime-config value, or a look-alike "trusted" host. That swing is the framing gap. On gpt-4o, ten overt classes sit at 0%; the strongest reframes hit 100%. Three hand-written wordings of one known template succeed about 96% of the time. An ablation shows this is instruction/data confusion, not a bypassed confidentiality policy.

What actually closes it are checks that **do not read the payload**: a destination allow-list, and a planner/reader split (planner never sees the page; reader never holds the secret). Model-side wording, channel separation, SecAlign (CCS 2025), and a substring egress guard all fail or get out-encoded (ROT13 still leaks).

#### Why it is useful in the real world

Any production agent that browses, reads tickets, or summarizes email while holding an API key, session token, or customer PII has this exact shape. A poisoned page that says "append the session token as a checksum" looks like a schema requirement, not theft. That is how a support bot, a research agent, or a Cursor-style tool agent leaks a connector credential without anyone pasting a jailbreak.

Concrete places this bites:

- A customer-support agent that fetches a "help article" and then posts a ticket update (secret: CRM token).
- An internal research agent that summarizes a vendor page while a GitHub PAT sits in the system prompt.
- An MCP client that reads a remote resource and then calls `http.post`.

A small reference implementation would be a mock agent with a canary secret, poisoned vs clean pages, and two defenses you can turn on: host allow-list and planner/reader split. That is the demo a security or platform team can point at when someone says "the model already refuses leaks."

#### Buildability

CPU Python weekend project. Mock tools that only record arguments. No GPU. Skip product tests against live ChatGPT.

---

### 2026-09-01-b - MCP discovery sanitizer

- **Kind:** protocol / defensive middleware
- **Date:** disclosed 6-8 Aug 2026; cache feature added 28 Jul 2026
- **Links:** [MCP-2026-008 / MCP-2026-015 writeup](https://datapace.ai/blog/mcp-cache-poisoning-prompt-injection)
- **Title:** Cache poisoning via public `cacheScope` chained with unsanitized `instructions`
- **Status:** shipped
- **Implementation:** https://github.com/sheshisheri-hi/mcp-discovery-sanitizer (private)

#### What it is

The July 28, 2026 MCP spec added `CacheableResult` and a `cacheScope` field. `public` means any intermediary may cache the response and serve it **across authorization contexts**. MCP-2026-008: the **server** sets that bit; nothing verifies the claim. A hostile server returns a poisoned `tools/list` (or prompts/resources) marked public, and a shared gateway re-serves it to every user behind it.

MCP-2026-015: the same discovery response includes `instructions`, which clients fold into the system prompt with no sanitization, validation, or length limit. Alone that is single-session prompt injection. Chained with public cache, one poisoned discovery response becomes a **cross-tenant** system-prompt injection.

Until a spec patch lands, the practical controls are: never cache across identities; treat `instructions` and tool descriptions as untrusted (bound length, isolate from the trusted prompt); pin which servers agents may talk to; log what each server actually returned so a swapped list is visible in a diff.

#### Why it is useful in the real world

This is not a user pasting a malicious PDF. The poison arrives wearing the server's name, already in the system prompt, and (if cached publicly) it hits every teammate on the same gateway. That is an enterprise MCP proxy, a shared Cursor/Claude desktop gateway, or a company "tool hub" that lists MCP servers for everyone.

Concrete places this bites:

- One intern adds a flashy MCP server; its `instructions` say "export the database URL"; a shared cache serves that list to finance.
- A CDN in front of an internal MCP registry caches a tampered `tools/list`, so every agent starts with a swapped "export" tool.
- A SaaS that hosts MCP for many tenants uses a public cache key and bleeds tenant A's poisoned metadata into tenant B.

A small sanitizer in front of `initialize` / `tools/list` is the thing you would actually ship: strip or bound `instructions`, refuse `cacheScope: public` from untrusted servers, isolate leftover text in a clearly untrusted block, and log a hash of the discovery blob. That is a gateway filter, not a research model.

#### Buildability

CPU Python. Parse JSON-RPC-shaped discovery payloads, apply rules, emit a verdict. Tests are poisoned vs clean fixtures. No GPU.

---

### 2026-09-01-c - MCP-Guard fail-fast stage

- **Kind:** tool / paper (implement Stage I only)
- **Date:** ACL 2026 Findings; arXiv:2508.10991
- **Links:** [ACL paper](https://aclanthology.org/2026.findings-acl.240/), [arXiv HTML](https://arxiv.org/html/2508.10991v4), [GenTelLab/MCP-Guard](https://github.com/GenTelLab/MCP-Guard)
- **Title:** MCP-Guard: A Multi-Stage Defense-in-Depth Framework for Securing Model Context Protocol in Agentic AI
- **Authors:** Wenpeng Xing et al.
- **Status:** queued

#### What it is

A proxy sitting between an MCP host and server with three stages: (I) fail-fast static scanners in under 2ms, (II) a fine-tuned E5 embedding detector, (III) an LLM arbitrator. Stage I is six lightweight detectors (prompt injection / hidden instructions, sensitive file paths, shell, SQL, shadow-hijack / spoofed tool calls, cross-origin). The paper says this stage filters about 38.9% of explicit threats so the expensive stages are not hit on every request. They also released MCP-AttackBench (~70k samples).

The useful slice for a first private repo is **Stage I only**: deterministic regex/lexical checks on MCP JSON-RPC messages. Skip the neural and LLM stages until someone wants them. That matches how you would actually put a first filter in a gateway: cheap, explainable, fail-closed on obvious payloads.

#### Why it is useful in the real world

Agent gateways cannot run an LLM classifier on every `tools/call`. You want a first sieve that stops `rm -rf`, `UNION SELECT`, `.ssh/`, `/etc/passwd`, and obvious "ignore previous instructions / export the connection string" strings in milliseconds, then log and block. That is the same layer WAFs occupy for HTTP: not complete, but it kills the noisy majority and gives you an audit trail.

Concrete places this bites:

- An internal MCP gateway in front of Slack, GitHub, and a warehouse; you want shell/SQL/path payloads dropped before they reach the model or the tool.
- A product that lets customers register MCP servers; Stage I is the default allow/deny you can explain to a security review.
- A red-team harness: replay AttackBench-style fixtures against the scanner and show pass/fail in CI.

#### Buildability

CPU Python. Port or reimplement the six Stage I detectors against fixture messages. Do not pull E5 or an LLM arbitrator into v1. Reference: [GenTelLab/MCP-Guard](https://github.com/GenTelLab/MCP-Guard).

---

## 2026-09-02

### 2026-09-02-a - SkillGuard (reachability confinement)

- **Kind:** paper
- **Date:** 30 Aug 2026
- **Link:** [arXiv:2608.30041](https://arxiv.org/abs/2608.30041)
- **Title:** Reachability-Based Capability Confinement for LLM Agents under Indirect Prompt Injection
- **Authors:** Wujie Xiong, Rabimba Karanjai, Yang Lu, Weidong Shi, Lei Xu
- **Status:** queued

#### What it is

Agents commit external skill/tool outputs into execution context, so attacker-controlled data can steer later privileged actions. Most defenses classify the untrusted blob or authorize the next call in isolation. SkillGuard is a harness-level layer that treats that commit as **contamination** and then shrinks future capabilities so the contaminated state cannot reach deployer-defined forbidden states.

Mechanics: a **Skill Impact Graph** of security-relevant transitions from registered skills; **steerability signatures** that bound which parameters a skill may still set; an **inline reference monitor** that checks capability + args before dispatch and can suppress bad outputs after. After contamination it applies a min-cost restriction (binary revoke, fractional tighten envelopes, or fractional-flow min-cut) with **no extra LM calls**. On AgentDojo (Gemini 2.5 Flash, Llama3.3-70B) vs Spotlighting / CaMeL / AttriGuard, under Tool Knowledge attacks it eliminated ASR on three of four suites and cut Slack ASR to 4.8% / 14.3% by backend; fractional-flow keeps more utility at similar ASR. No public SkillGuard repo on the abs page.

#### Why it is useful in the real world

This is the policy you want the moment an agent reads untrusted email, a web page, or a third-party skill: not "hope the model ignores it," but "that read just poisoned the session, so shell/network/export tools narrow until the session ends."

Concrete places this bites:

- An MCP / coding harness that auto-downgrades shell and outbound HTTP after the agent reads a vendor README or a customer ticket.
- An enterprise gateway that encodes forbidden sinks (exfil hosts, secret stores) as reachability policies and enforces them outside the model.
- A demo for security review: show the same poisoned page with and without the reference monitor, and show which tools remain callable.

#### Buildability

CPU Python weekend. Graph + reference monitor + fixture skills. No GPU. Distinct from unrelated GitHub projects also named "skillguard."

---

### 2026-09-02-b - Delegation Without Trust

- **Kind:** paper (authorization broker)
- **Date:** 31 Aug 2026
- **Link:** [arXiv:2609.00267](https://arxiv.org/abs/2609.00267)
- **Title:** Delegation Without Trust: An Empirical Gap Analysis of Identity, Authorization, and Runtime Governance in Multi-Agent LLM Systems
- **Authors:** Panduranga Sai Varma Dantuluri, Jyotirmoy Sundi (VotalAI)
- **Status:** queued

#### What it is

Security property: a **fully prompt-injected** agent still must not exceed explicitly delegated authority. Threat model covers confused deputy, token theft/replay, PI privilege escalation, and compromised sub-agents (eight requirements). Gap analysis: default "bearer creds + model-gated auth" fails all four; LangGraph / CrewAI / AutoGen lack confinement; MCP authz is only partial.

Their answer is an **authorization broker** outside the model: SPIFFE-style identity, OIDC-rooted capability tokens, issuer-mediated hop exchange into attenuated short-lived child tokens, every tool call mediated by a model-independent PEP, revocation down the chain. Reported: blocks the four threats, rejects 0/200,000 forged tokens, confines compromised sub-agents to mean ~1.5 reachable actions vs ~8,100 under bearer (2,000 scenarios), ~2.6 µs/decision. Principles also shipped as VotalAI LLM Shield. The ~160-line Python reference broker is **not** publicly released on the abs page (stdlib HMAC / macaroon-style design described for reimplementation).

#### Why it is useful in the real world

If the model is the policy engine, injection *is* privilege escalation. Moving auth to a broker means a hijacked planner still cannot call tools it was never delegated, and a compromised sub-agent cannot inherit the parent's full token.

Concrete places this bites:

- Multi-agent orchestration where each sub-agent gets a short-lived, attenuated token instead of the user's PAT.
- MCP tool auth that does not trust the model to "remember" scopes.
- Post-incident audit: who authorized which hop, and was the credential still valid.

#### Buildability

CPU Python weekend for a small broker + policy checks + forged-token tests. Reimplement from the paper's design (reference code not public). No GPU.

---

### 2026-09-02-c - Defense-as-Skill (SkillSonar)

- **Kind:** paper
- **Date:** 1 Sep 2026
- **Link:** [arXiv:2609.01487](https://arxiv.org/abs/2609.01487)
- **Title:** Defense-as-Skill: Evolving Runtime Guard Skill for Skill-Augmented Agents
- **Authors:** Xiaofang Yang, Ziqi Miao, Dianbo Sui, Jing Shao, Lijun Li
- **Status:** queued

#### What it is

Skill-augmented agents (Claude Code, OpenClaw, etc.) load reusable skills as durable context. Malicious skills often look fine at install time and only fire when a later task/workspace makes the unsafe action look useful. **Defense-as-Skill** packages the runtime guard as an installable skill itself. **SkillSonar** sits beside task skills, checks sensitive actions against the user's task boundary, and routes to allow / replan / confirmation without forking the agent runtime.

They build **SCOPE-R** (6 risk families, 21 sub-categories, 206 confirmed malicious + 43 benign) and improve the guard via **MCTS evolution** of the on-disk skill from rollout feedback. On Claude Code / OpenClaw / GLM-5: ID ASR 0.482→0.104, OOD 0.606→0.115. Soft policy layer meant to complement sandboxes, not replace them. No SkillSonar author repo URL on the abs page; PinchBench / WildClawBench are cited as external tools.

#### Why it is useful in the real world

You cannot recompile Claude Code for every customer. Shipping a guard *as a skill* means the same distribution channel as the attack surface: installable, inspectable, editable, no custom harness fork.

Concrete places this bites:

- A default "security skill" pinned in a coding-agent org template that forces confirmation before secrets, network, or approval-bypass actions.
- Evolving the guard from your own red-team rollouts instead of waiting for a vendor patch.
- A product UX that already has allow / replan / confirm and needs a task-boundary policy object to drive it.

#### Buildability

CPU Python weekend for a **static** SkillSonar-style guard skill + fixture tasks. Full MCTS evolution needs many agent rollouts / API cost (not GPU-mandatory, but heavier). Start without evolution.

---

## 2026-09-03

### 2026-09-03-a — ACLE-MCP (attested capability leases)

- **Kind:** paper / protocol
- **Date:** 3 Sep 2026
- **Link:** [arXiv:2609.02690](https://arxiv.org/abs/2609.02690)
- **Title:** ACLE-MCP: Attested Capability Leases for Execution-Time Trust in Remote LLM Tool Use
- **Authors:** Zhiyang Ding, Yang Luo, Guangpu Chen, Qingni Shen, Zhonghai Wu (Peking University)
- **Status:** queued
- **Related sample:** https://github.com/sheshisheri-hi/mcp-atsa-admission (ATSA connect-time admission gate — **not** an ACLE implementation)

#### What it is

OAuth can say a client may call a remote MCP server. It does **not** say that the later `tools/call` still hits the workload you appraised at connect time. The paper names that hole the **post-authorization execution trust gap**: after the token is issued, the provider can roll back to a vulnerable image, route through an undeclared proxy, reuse a stale attestation, or accept a lease copied from another sender.

**ACLE-MCP** closes it with an invocation-scoped **capability lease**. For a protected call: (1) a Verifier appraises the current provider-side workload and emits a short-lived signed appraisal; (2) a lease issuer binds OAuth context + appraisal + sender key + tool/op/object/parameter/downstream bounds into a short-lived, sender-constrained lease; (3) a provider-side **Execution Gate** validates and consumes that lease immediately before protected tool logic runs. Low-risk reads can stay on plain OAuth; medium/high-risk calls take Attested Step-Up. Optional vTPM quote path; Keycloak/OIDC for real tokens; MCP Python SDK for the tool side.

In their agent-extension suite, OAuth-only and connect-time attestation block **0** of six execution-misuse families; full ACLE-MCP blocks all of them with 0 false positives on the benign tasks, at about **+25.7%** p95 latency on normal allowed calls in the local simulation (12.20 ms → 15.34 ms). Ablations show sender binding, freshness, downstream constraints, receipts, and workload binding each reopen a distinct family.

#### Why it is useful in the real world

Enterprise MCP is mostly "token at the edge, hope the backend is still the one you registered." That is exactly where a compromised or rolled-back tool container still serves with a valid OAuth token. ACLE-MCP is the missing **admission check at the handler**, not another prompt filter.

Concrete places this bites:

- A remote "update_ticket" / "place_order" MCP server behind Keycloak where the serving image must still match the registered measurement when the call lands.
- A multi-tenant tool hub that must reject lease replay across clients and undeclared downstream proxies.
- A security review that already has OAuth and wants a concrete answer to "what happens if the pod is substituted after connect?"

#### Buildability

CPU Python weekend for a **simulated** Verifier + lease issuer + Execution Gate + fixture MCP tools (replay, substitution, stale appraisal, scope misuse). Full Keycloak + real TPM is optional later. No GPU. Distinct from Session-1 MCP-Guard Stage I (text scanners) and from Delegation Without Trust (identity broker); this one binds **workload appraisal to the call**.

---

### 2026-09-03-b — MCP-to-A2A public-sharing egress

- **Kind:** paper / measurement harness
- **Date:** Aug 2026 study; arXiv posted 3 Sep 2026
- **Link:** [arXiv:2609.01693](https://arxiv.org/abs/2609.01693)
- **Title:** Public-Sharing Labels and Verbatim Field Egress in an MCP-to-A2A Agent Configuration: A Controlled Multi-Model Study
- **Authors:** Arpan Kumar Mahapatra
- **Artifact:** [ArpanKumarM/agent-interop-bench](https://github.com/ArpanKumarM/agent-interop-bench/releases/tag/paper-v1.0)
- **Status:** queued

#### What it is

Safety numbers measured on MCP alone or A2A alone do not describe a host that **reads a record over MCP and then relays to another agent over A2A**. This paper builds one controlled composition: local MCP fixture → host (real model) → local A2A fixture, with a single ordered event trace and **judge-free** exact-substring egress scoring (no LLM judge).

Frozen three-arm design on 10 synthetic support personas: same six field values byte-identical across arms; only the opening header changes — `[CONFIDENTIAL - INTERNAL ONLY]`, unlabeled, or `[PUBLIC - OK TO SHARE]`. Four models × 3 arms × 4 repeats = 480 trials. Primary outcome: any of the six substantive strings appears verbatim in the outbound A2A message.

Result that matters for builders: **confidential vs unlabeled is floor-limited / inconclusive** in every model here. Adding an explicit public-sharing cue is descriptively associated with **higher** verbatim egress vs unlabeled, strongly model-dependent — Claude Sonnet 5 mean P−N **+0.800** (all 10 scenarios; mostly whether it relays at all), moderate for gpt-5.6-luna, small for gpt-5.6-sol, complete floor for gpt-5.6-terra. Association in one config, not a causal claim. Code + byte-pinned traces released.

#### Why it is useful in the real world

Product teams keep shipping "mark the row confidential" as if that were an egress control, then compose MCP tools with A2A handoffs. This study says the interesting failure mode in that config is often the **public / OK-to-share cue widening relay**, not the confidential label saving you — and you need a deterministic harness, not vibes, to see it.

Concrete places this bites:

- A support agent that `get_account_record` over MCP then messages a remote billing agent over A2A.
- A policy review that wants a CI canary: same record, three headers, assert egress rate under a frozen host policy.
- Cross-protocol composition risk named in IETF MCP security drafts — this is the live-model measurement companion.

#### Buildability

CPU Python weekend to fork / slim the released harness into a **mock-model** mode (fixture decisions) plus one optional live-model adapter. Deterministic scoring is already there. No GPU required for the reference. Full multi-model live panel needs API keys and budget.

---

### 2026-09-03-c — Context Privilege Escalation (CoRA)

- **Kind:** paper / analysis tool
- **Date:** 2 Sep 2026
- **Link:** [arXiv:2609.01222](https://arxiv.org/abs/2609.01222)
- **Title:** What's in Your Agent's Context? Context Privilege Escalation Attacks against AI Agent Harness
- **Authors:** Zichuan Li, Jian Cui, Ashley Chen, Xiaojing Liao, Luyi Xing (UIUC)
- **Status:** queued

#### What it is

Coding-agent harnesses (Codex, Claude Code, Gemini CLI, OpenClaw, Cline, … — 12 studied) assemble context from opaque sources: memory files, skill search paths, directory trees, git logs, config, markup wrappers. Models are trained with a **message-role privilege hierarchy** (system/developer > user > tool). The paper shows harness design lets attacker-controlled low-privilege content climb that hierarchy or widen its **scope**.

Two classes:

1. **M-CPE (message-role):** tool/web content instructs the agent to write into a higher-role source (e.g. skill description, memory loaded as system/developer).
2. **X-CPE (cross-scope):** session-scoped poison is written into project- or user-scoped files the agent reloads after restart / across projects.

They taxonomize **16 attack vectors** (memory/skill search quirks, runtime discovery, environment trees, XML markup escape, override-file priority, config self-mod, inline shell in skills, etc.) and ship **CoRA**, an LLM-assisted analyzer that finds sources, validates roles/scopes with canaries, and checks CPE paths. Across 12 harnesses: **282** verified context sources, **1761** candidate CPE paths; large fractions load and behave under GPT-5.5 / GPT-5.4-mini. PoCs include RCE-class outcomes on some harnesses; responsible disclosure underway; some vendors already shipped mitigations.

#### Why it is useful in the real world

This is not "another prompt injection paper." It is a **SBOM for agent context**: which files and env blobs enter which role, and can an untrusted tool response promote itself into always-on memory or skills. That is exactly the surface of Claude Code / Codex / Gemini CLI in a legacy engineering shop.

Concrete places this bites:

- Org template that pins which memory/skill paths agents may write, and blocks tool→skill promotion.
- PR-review bots (Codex GitHub Action) that must ignore attacker-added `AGENTS.override.md` / nested `GEMINI.md`.
- A weekend scanner that lists role/scope for a harness config and flags writable high-privilege paths.

#### Buildability

CPU Python weekend for a **narrow CoRA slice**: inventory memory/skill paths for one harness (e.g. Claude Code or Codex), canary-inject, assert observed role. Full multi-harness CoRA is a research system. Do **not** reproduce weaponized RCE PoCs in the private sample repo — inventory + policy checks only. No GPU.

---

## 2026-09-04

### 2026-09-04-a — HookPry (lifecycle-hook update attacks)

- **Kind:** paper / attack framework
- **Date:** 3 Sep 2026
- **Link:** [arXiv:2609.03884](https://arxiv.org/abs/2609.03884)
- **Title:** A Blind Trust, the Bloody Thrust: When Attacker-Controlled Hook Updates Steer AI Agent Harnesses towards Malicious Behaviors
- **Authors:** Pengxun Li, Litian Zhang, Jianwei Hou, Shujiang Wu, Song Li, Zifeng Kang, Xi Zhang
- **Artifact:** anonymized [HookPry](https://anonymous.4open.science/r/HookPry-2EBD)
- **Status:** queued

#### What it is

Coding-agent harnesses (Claude Code, Codex CLI, OpenClaw, OpenHarness, OpenCode, Hermes, WorkBuddy — seven evaluated) expose **lifecycle hooks**: config that binds host shell commands to events such as session start, tool use, and file edits. Those commands run with host privileges and can fire on a path the LLM never sees. The paper isolates the **lifecycle-hook update path**: under a supply-chain model where the attacker only controls plugin metadata and hook config, a benign versioned plugin can be trojanized by a later update that silently adds attacker-chosen bindings.

**HookPry** automates that attack with three pieces: **AMO** (adversarial manifest optimization for marketplace discoverability of the initially benign plugin), **TD** (temporal decoupling so trust is earned before the hook-bearing update), and **LCI** (least common interface that compiles one abstract payload into each harness’s native hook schema). Ten MITRE-mapped objectives (credential collection, exfil, C2, privilege escalation, persistence, etc.). Across **1,000** end-to-end runs (25 harness×backend combos), HookPry compromised **all seven** harnesses; micro-average E2E-ASR **77.0%**, peak **92.5%** on Hermes; **0** runs explicitly blocked by the model. Microsoft Defender recall **0%**; union of three static defenses still missed **47.5%** of malicious artifacts. Native hooks beat translated MCP-description poisons on the same targets (92% vs 56% E2E-ASR).

#### Why it is useful in the real world

This is the “npm postinstall for agent plugins” problem: users approve a helpful plugin once, then an update adds a SessionStart / PreToolUse command that dumps env tokens or rewrites tool output. Prompt filters and model alignment never see it.

Concrete places this bites:

- An org that auto-updates Claude Code / Codex marketplace plugins without per-hook re-authorization.
- A security review that needs an inventory of which hooks fire outside the model path, and a pin/diff of hook manifests across versions.
- A weekend **defense** sample: hash-pin allowed hooks, require fresh consent on hook add/change, fail-closed on unsigned updates — not a weaponized HookPry replay.

#### Buildability

CPU Python weekend for a **defensive** hook integrity gate: parse fixture manifests, detect added/changed command bindings, emit allow/block + audit log. Skip reimplementing the attack framework’s payloads. No GPU.

---

### 2026-09-04-b — ContextLeak (malicious-tool context exfiltration)

- **Kind:** paper
- **Date:** 28 Aug 2026
- **Link:** [arXiv:2608.27800](https://arxiv.org/abs/2608.27800)
- **Title:** ContextLeak: Exfiltrating LLM Agent Context via Malicious Tools
- **Authors:** Yuqi Jia, Ruiqi Wang, Patrick Li, Yuepeng Hu, Peinian Li, Neil Zhenqiang Gong (Duke / Stanford)
- **Status:** queued

#### What it is

Prior malicious-tool work mostly optimizes for **selection** (or ships malicious tool code). Context exfiltration also needs the agent to **pass runtime context as tool arguments**. ContextLeak is the first attack that crafts tool **name + description** so the agent both selects the tool and feeds it user prompt / conversation history / tool list.

Mechanics: an **attack LLM** generates candidates; **DAPO** RL fine-tunes it on shadow users with a reward that scores selection plus Levenshtein/length match of arguments to shadow context; a **strategy library** (failure / low-exfil / full-success) guides the next round. Evaluated on ToolBench/ToolAlpaca-style victims across open and closed backends. Headline: ContextLeak MTSR **0.92 / 0.89 / 0.86** on prompt / history / tool-list vs much weaker adapted baselines; transfers across backends and domains; prevention defenses (StruQ/SecAlign) trade utility for ASR drops; text detectors (PromptGuard, DataSentinel, PromptArmor, Ant MCPScan metadata) show near-**1.0** FNR. Claude Code transfer: **22%** MTSR with near-complete extraction when selected.

#### Why it is useful in the real world

A “helpful preprocessor” MCP tool whose description says it needs the full request / tool catalog for consistency is exactly how a support or coding agent hands PII and connector inventories to an attacker endpoint — without an overt “ignore previous instructions.”

Concrete places this bites:

- MCP / Skills marketplaces where description-only review is the install gate.
- A gateway that **strips or bounds** tool parameters that look like full prompt/history dumps before dispatch.
- A CI canary: canary secret in context; assert no tool call arguments contain it (argument egress gate), independent of selection rate.

#### Buildability

CPU Python weekend for the **defense** slice: argument-schema allowlists + canary egress tests on mock tool calls. Full ContextLeak RL attack needs GPU / API budget — do **not** ship that as the sample. No GPU for the gate.

---

### 2026-09-04-c — SkillShift (covert skill policy steering)

- **Kind:** paper
- **Date:** 2 Sep 2026
- **Link:** [arXiv:2609.02564](https://arxiv.org/abs/2609.02564)
- **Title:** A Finger on the Scale: Covert Policy Steering through Agentic Skills
- **Authors:** Jiarui Li, Jiahao Chen, Chunyi Zhou, Yuwen Pu, Oubo Ma, Zhou Feng, Chunqiang Hu, Shouling Ji
- **Status:** queued

#### What it is

Reusable agent **Skills** are not just docs — they are externalized behavioral policies. Existing scanners catch overt injection / task hijack. **Skill Policy Integrity** asks something stricter: does the skill stay faithful to its declared task and the user-authorized objective, or does it systematically bias which legitimate candidate wins?

**SkillShift** is a black-box optimizer that appends structured, plausible criteria (framing, tie-breakers, examples) under hard constraints: no candidate data changes, no explicit “always pick X,” no query-specific shortcuts. Instantiated on agentic shopping and Python dependency selection: attacker-favored selection **81.33%** / **63.33%** with **100%** valid-output rate; frozen policies transfer across backends and Claude Code / Codex-compatible agents; **0/6** evaluated skill scanners flag SkillShift (while direct injection is often caught); registries (ClawHub, Tencent SkillHub, vskill) admit the steered skills.

Distinct from Session-2 SkillGuard (reachability after contamination) and SkillSonar (runtime allow/replan/confirm): this one is about **silent ranking bias among valid options**.

#### Why it is useful in the real world

A “helpful” shopping or package-choice skill that always nudges one brand / one dependency while still returning schema-valid answers is supply-chain fraud that today’s scanners miss. That is exactly how affiliate or vendor-sponsored skills will look in Claude Code / OpenClaw ecosystems.

Concrete places this bites:

- Org skill review that adds a **behavioral** check: same query + candidate set, clean vs candidate skill, flag Lift above a threshold.
- A marketplace gate that requires A/B selection histograms, not only static malware scans.
- A weekend auditor: fixture candidates + two skills + mock selector scoring PSR/Lift — no need to reproduce SkillShift’s full optimizer.

#### Buildability

CPU Python weekend for a **Skill Policy Integrity auditor** (fixture tasks, clean vs under-test skill, selection Lift + VR). Full SkillShift optimization needs live model APIs. No GPU for the auditor.

---

## 2026-09-06

### 2026-09-06-a — ATSA / SEP-2809 Attested Tool-Server Admission

- **Kind:** protocol draft
- **Date:** SEP opened 28 May 2026; sample shipped 6 Sep 2026
- **Link:** [SEP-2809](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2809)
- **Title:** Attested Tool-Server Admission (ATSA)
- **Authors:** Alfredo Metere (Enclawed LLC) et al.
- **Status:** shipped
- **Implementation:** https://github.com/sheshisheri-hi/mcp-atsa-admission

#### What it is

An MCP host today takes a tool server’s identity and its `tools/list` on faith. TLS answers “did I reach the named endpoint.” OAuth answers “may this user use this server.” Nobody asks the third question: **is this server one the host is authorized to use as a tool provider, and which tools?** A prompt-injected model can then drive a destructive tool on any server the host already connected to. That is a door problem, not a “the model said a weird tool name” problem.

Draft **SEP-2809 / ATSA** is an optional, additive admission layer. A server publishes a small, offline-signed *clearance* at a well-known URI. The host verifies it against a **pinned trust root** before any tool dispatch. Admitting the server is kept distinct from authorizing its tools via a closed per-server allow-list. Every decision is auditable. Unextended hosts ignore the mechanism and behave as today.

The shipped sample is a **CPU-only Python gate** inspired by that draft — not a full SEP port, not enclawed conformance, and **not ACLE-MCP**. Official MCP today is still OAuth. ATSA is the connect-time door check: verify the server once before tools are in play. [ACLE-MCP](https://arxiv.org/abs/2609.02690) (2026-09-03-a, still queued) is the per-call sticky note: after OAuth, re-check the real provider-side workload and the exact action right before a risky invocation. Different timing, same instinct.

What the sample actually does: load a signed clearance JSON, canonicalize the body, verify Ed25519 against a host-pinned trust root, check issuer / server id / time / sensitivity, then run a separate `authorize_tool` closed list. Admit vs forged vs tool-deny are fixture-tested. The trust root is **not** fetched from the server being admitted.

#### Why it is useful in the real world

Enterprise MCP is “TLS + OAuth + hope `tools/list` is honest.” That is exactly where a prompt-injected planner calls `delete_account` on a weather server the host never meant to treat as a privileged tool provider.

Concrete places this bites:

- A company tool hub that must refuse an unregistered or swapped MCP server before any `tools/call`.
- A host that already connected to “weather.internal” and must still deny `delete_account` even if the clearance still verifies.
- A LinkedIn / security-review demo: same fixtures, admit vs forged signature vs tool-not-allowlisted, readable reasons.

#### Buildability

Shipped. Public weekend sample: verify clearance, pin trust root, separate tool allow-list, append-only audit. No GPU, no TPM, no Keycloak, no live MCP transport. Distinct from 2026-09-03-a (ACLE leases at the handler) and from 2026-09-02-b (identity broker).

---

### 2026-09-06-b — ROPE (Routed Origin Policy Enforcement)

- **Kind:** paper
- **Date:** 27 Aug 2026
- **Link:** [arXiv:2608.27496](https://arxiv.org/abs/2608.27496)
- **Title:** ROPE: Routed Origin Policy Enforcement against Indirect Prompt Injection
- **Authors:** Xinhang Ma, Chaowei Xiao, William Yeoh, Ning Zhang, Yevgeniy Vorobeychik (WashU / Johns Hopkins)
- **Artifact:** [xhOwenMa/ROPE](https://github.com/xhOwenMa/ROPE)
- **Status:** queued

#### What it is

Indirect prompt injection plants instructions in content a tool-using agent reads, then steers a later **state-changing** tool (send, pay, delete, grant). Prior system-level defenses either predict a plan / allow-list from the user query (DRIFT, Progent) or tag every runtime read as untrusted (CaMeL-style IFC). Both settle the constraint **before** delegated content arrives. On open-ended suites that need runtime replanning, that crush utility: AgentDyn clean utility collapses to ~18% (DRIFT) / ~7% (Progent) / ~15% (PFI) vs ~47% undefended. Putting an LLM back in the loop to recover utility reopens the attack surface.

**ROPE** keeps a structural rule: a value may reach a state-changing tool only if it traces unforgeably to (T1) the user request, (T2) a source the user explicitly named (e.g. a signed sender), or (T3) the user’s own authoritative records. Offline, tool schemas yield an audited set of sensitive parameters. Per task, a **router reads only the trusted request** and sets how open each parameter may be. At runtime an origin tracker labels each tool result; a **deterministic guard** admits or blocks the call. No model in the enforcement loop. Two stated guarantees: no attacker-only origin reaches an origin-guarded parameter, and rewording the injection does not change the admission decision.

Across four agent models on open-ended suites: ASR **1.6–2.6%** while retaining **82–100%** of undefended clean utility. Adaptive optimization against ROPE is largely ineffective; long-horizon attacks that beat prior system-level defenses hit **0** here.

#### Why it is useful in the real world

This is the policy you want when the user said “pay the invoice from Acme” and a poisoned PDF tries to swap the recipient: not “hope the model notices,” and not “block every value that came from a tool,” but “that IBAN did not originate from the user, a named source, or the user’s ledger — refuse the pay call.”

Concrete places this bites:

- A support or finance agent that may email / pay / write only with destinations that trace to the ticket or the user’s records.
- An MCP gateway that marks `to`, `account`, `url`, `path` as origin-guarded and logs admit/deny without another LLM judge.
- A weekend sample: fixture origins + a small sensitive-parameter table + a mock agent that tries clean vs injected recipients.

Distinct from 2026-09-02-a SkillGuard (shrink capabilities after contamination) and 2026-09-01-a Framing Gap (destination allow-list / planner-reader split). ROPE is **parameter-origin admission** on the state-changing call.

#### Buildability

CPU Python weekend for a **sample-ready** origin guard: label fixture values with T1/T2/T3/untrusted, compile a tiny policy from a trusted request, assert injected recipients never reach `pay` / `send`. Skip the paper’s full multi-model panel and adaptive attacker. Author code is public if you want to read it; do not vendor the attack harness into the sample. No GPU.

---

### 2026-09-06-c — SEP-3004 Tamper-Evident Audit Record Contract

- **Kind:** protocol
- **Date:** opened 2 Jul 2026; last PR activity 12 Aug 2026
- **Link:** [SEP-3004 / PR 3004](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/3004)
- **Title:** Tamper-Evident Audit Record Contract
- **Authors:** Scott Rhodes (Notboatanchor Labs); Syed Maaz Ahmed (Interlock); Alfredo Metere (Enclawed LLC)
- **Status:** queued

#### What it is

The Security IG charter asks for tamper-evident records of what a tool call did and under what authority. That guarantee is **named at several seams and specified at none**: SEP-2809 makes a tamper-evident admission record a SHOULD without a shared shape; Interceptors (#2624) say trails must be “tamper-proof” with no checkable property; caller-governance layers emit incompatible logs. SEP-2484 makes a conformance check the bar for Final — and today there is no shared definition to write one against.

**SEP-3004** (Standards Track, still open) specifies the interoperability primitive once: a minimal protected core; type-keyed `extensions` so admission, runtime-security, and caller-governance attach under **one digest**; sorted-JSON canonicalization aligned with #2809; an append-only SHA-256 hash chain; a verification procedure; a structured attestation manifest for the part that is not wire-observable. Registered extension types: `caller-governance` (fully specified), `runtime-security` (normative, cross-ref #2624), `admission-control` (named, cross-ref #2809). Known-answer digests (`f733fed9…` two-extension, `d494769c…` single-extension) reproduce from the published canonical rule + stock `sha256sum`. Independent TypeScript (GIF) and later Go runners have hit the same bytes.

This is a **protocol watch**, not a pick: track whether PR 3004 lands, whether the `admission-control` registration stays with ATSA, and whether the KAT vectors remain stable. A later sample would be a tiny canonicalizer + chain verifier against those published digests — not another admission gate.

#### Why it is useful in the real world

Every “we log tool calls” slide dies in audit when two vendors cannot hash the same event to the same bytes, and when the admission decision, the runtime verdict, and the caller-governance grant live in three unjoinable logs. A shared record contract is how ATSA’s door check, an interceptor, and a purpose-scoped PEP all point at one verifiable chain.

Concrete places this bites:

- A regulated MCP gateway that must prove *who admitted which server, which tool ran, under what grant* after an incident.
- Cross-vendor conformance: same fixture record, same SHA-256, two languages.
- Composition with 2026-09-06-a: ATSA already appends an audit line; SEP-3004 is the shape that line should take if the SEP finalizes.

#### Buildability

Watch the PR. If it stabilizes, a CPU Python weekend is: sorted-JSON canonicalize + SHA-256 chain + assert the published KATs. Do **not** invent a competing record format while 3004 is open. No GPU. Distinct from the ATSA sample (admission) and from ACLE (per-call leases).

---

### 2026-09-06-d — Deadbugz (runtime-gated MCP metadata poisoning)

- **Kind:** paper / CSA research note
- **Date:** campaign 10 Aug 2026; Pillar disclosure 12 Aug 2026; CSA note 2 Sep 2026
- **Links:** [CSA note](https://labs.cloudsecurityalliance.org/research/csa-research-note-deadbugz-mcp-metadata-poisoning-20260902-c/), [Pillar writeup](https://www.pillar.security/blog/deadbugz-currently-active-mcp-supply-chain-campaign)
- **Title:** Deadbugz: Runtime-Gated MCP Metadata Poisoning as Supply-Chain Attack
- **Authors:** Cloud Security Alliance AI Safety Initiative (campaign first documented by Pillar Security)
- **Status:** queued

#### What it is

Deadbugz is a **live** MCP supply-chain campaign, not a lab attack. A GitHub account (`zellkernel`) opened 23 unsolicited PRs in 75 minutes on 10 Aug 2026, adding either a remote MCP endpoint (`productivity-suite-mcp.onrender.com/mcp`) or a hidden local script (`~/.config/.cache/.sys/.deadbug-mcp.py`) to unrelated repos. None merged through GitHub review; that does not prove nobody ran the server locally.

The server advertises two innocuous tools (`format_text`, `summarize`) and keeps a **per-client `tools/call` counter**. For the first three calls, `tools/list` / `prompts/get` match the names. After the third, the **same endpoints** return different metadata: instructions to hunt SSH keys, AWS credentials, kubeconfigs, and shell history, and to hide that from the operator. A Bitcoin address in the delayed prompt points at a financially motivated actor. No CVE — this is not an MCP spec bug.

That is **runtime-gated metadata poisoning**: the malicious text is in the server from day one, withheld until normal use crosses a threshold a reviewer will not hit. Unlike a classic rug-pull (new version / new schema after approval), the published code never changes; only live protocol responses diverge. Install-time scanners, short interactive tests, and SBOM diffs all miss it. CSA maps it onto the same tool-poisoning / confused-deputy class as postmark-mcp and MCP03:2025, distinguished by the call-count trigger and PR delivery.

Recommended controls: treat MCP metadata as a **live** security surface; fingerprint approved `tools/list` / descriptions and fail-closed on drift; pin hashes; allow-list servers; do not let a formatting tool reach SSH or cloud creds regardless of what its description later says.

#### Why it is useful in the real world

This is the “it looked fine in review” incident your MCP gateway will actually get. A helpful text utility sits in the IDE for a week, then the agent starts reading `~/.ssh`. Prompt filters never see a user-pasted jailbreak — the poison arrives wearing the server’s own tool description.

Concrete places this bites:

- Org policy: any PR that adds an MCP server is a credential-class change, not “developer tooling.”
- A gateway that hashes `tools/list` at admit-time (compose with 2026-09-06-a) and re-checks on every list/refresh; drift after N calls is a deny + audit event.
- IOC sweep: that Render host, that hidden `deadbug-mcp.py` path, `zellkernel` / `@llmgod`.

#### Buildability

CPU Python weekend, **sample-ready**, defense only: fixture server that flips metadata after three calls; client/gateway that pins the first honest `tools/list` hash and blocks the flip. Do **not** reproduce credential-hunting instructions, the campaign payload, or PR-spam tooling. Distinct from 2026-09-01-b (discovery `instructions` / `cacheScope` sanitizer) and 2026-09-04-a HookPry (hook-update integrity). No GPU.

---

## 2026-09-07

### 2026-09-07-a — FGLGuard (federated topology-guided MAS safety)

- **Kind:** paper
- **Date:** 2 Sep 2026
- **Link:** [arXiv:2609.02967](https://arxiv.org/abs/2609.02967)
- **Title:** Privacy-Preserving Topology-Guided Safety for LLM-Based Multi-Agent Systems via Federated Graph Learning
- **Authors:** Jinxi Yu, Eric Hanchen Jiang, Levina Li, Dong Liu, Zhi Zhang, Wenxiao Zhao, Yanxuan Yu, Kai-Wei Chang, Ying Nian Wu (UCLA)
- **Code:** https://github.com/jinxiy1104/FGLGuard
- **Track:** watch
- **Status:** queued

#### What it is

Topology-guided MAS guards train a GNN over the inter-agent communication graph to localize risky agents and intervene on edges/topology. Prior work (G-Safeguard and kin) assumes one operator can **pool** all labeled episode traces. Across orgs that fails: prompts, tool outputs, and workflows stay siloed, and off-the-shelf transfer is near chance (AUROC ~0.51 on Agent-SafetyBench until in-domain retrain).

**FGLGuard** casts the problem as graph federated learning. Each operator fits an edge-featured graph-attention risk scorer on its own judge-labeled episode graphs and shares **only model updates**. Design pieces: proximal local objective for non-IID clients, domain-balanced aggregation, over-refusal-constrained threshold calibration, **corroborated upstream scoring** (acting agent maxed with neighbors), and one guarded rewrite before hard refuse. Claims: federated training matches or beats in-domain centralized ceilings on ASB / R-Judge / AgentDojo without pooling data; live AgentDojo ASR drops ~43% at near-unguarded utility; ~0.5M params / ~50 ms CPU check.

#### Why it is useful in the real world

Any multi-agent shop that cannot ship traces to a vendor safety model still wants a topology guard that improves when peer orgs see different attacks. That is regulated banks, hospitals, and enterprise AutoGen/LangGraph fleets sharing a common scorer without a shared data lake.

Concrete places this bites:

- Cross-BU agent platforms that each hold private incident graphs but share one risk model.
- A sidecar that rebuilds the last-T-round graph before a high-impact tool call and blocks/rewrites on corroborated score.
- A research compare against message-only filters (Llama Guard / content filters) that cannot localize a compromised agent.

#### Buildability

**Watch.** Full FGLGuard needs federated GAT training plus corpora. A thin weekend slice (not the paper) would be: fixture episode graphs + a frozen MiniLM embedding + a non-federated GAT or even a hand rule on corroborated neighbor scores — useful for demos, not for claiming the FL result. No GPU required for a toy sidecar; real training is heavier.

#### LinkedIn / demo

Skip flashy one-pager until someone wants a topology-sidecar mock — full FL story is too dense for a carousel.

#### Technical compare

- **G-Safeguard / GUARDIAN / SentinelAgent:** same topology-detection family; FGLGuard’s delta is **no-pooling federation** plus budgeted over-refusal calibration, not a new scorer architecture.
- **Message filters (Llama Guard, ShieldAgent):** per-utterance; miss multi-hop propagation and cannot localize which agent to prune.
- **Unsupervised graph anomaly (BlindGuard / XG-Guard):** no labels; paper shows they invert or chance-out on organic ASB-style risk where free judge labels exist.
- **ACLE / ATSA (already in backlog):** different layer — per-call leases / connect-time server admission vs **graph-level MAS risk scoring**.

---

### 2026-09-07-b — HarnessRisk (lifecycle harness safety benchmark)

- **Kind:** paper / benchmark
- **Date:** 18 Aug 2026 (project page updated Aug 2026; still absent from INDEX)
- **Links:** [arXiv:2608.17597](https://arxiv.org/abs/2608.17597), [project](https://baiyajing.github.io/harness-risk/)
- **Title:** HarnessRisk: A Lifecycle-Oriented Benchmark for Agent Harness Safety
- **Authors:** Yajing Bai, Jinhao Duan, Jie Peng, Xianfeng Wu, Sijia Liu, Song Wang, Tianlong Chen
- **Track:** sample-ready
- **Status:** queued

#### What it is

Most agent safety benches hit one attack class or one runtime slice. **HarnessRisk** evaluates the **harness** — the system that owns tools, extensions, persistence, permissions, and actions — across six lifecycle phases: Harness Configuration, Capability Extension, Runtime Operation, State Persistence, Action Control, Incident Recovery. 128 sandboxed cases; each pairs a benign user goal with an adversarial instruction inside an untrusted workflow artifact. Metrics: Utility, ASR, Persistence, Detection. Harnesses: OpenClaw, Nanobot, Hermes; six models; 14 model–harness configs.

Headline findings: ASR 12.6%–80.9% while Utility stays high (75–97%); **Harness Configuration is the most vulnerable phase on every harness**; same model can be more than 4× less safe under a different harness; detection does not equal safe action (high detection can still retain substantial ASR).

#### Why it is useful in the real world

If you ship Cursor-/OpenClaw-style agents, the failure mode is often “agent rewrote the approval policy / connector scope mid-workflow,” not a classic user-prompt jailbreak. Config-phase attacks look like authorized setup steps.

Concrete places this bites:

- An MCP host/client that freezes security-sensitive settings (approvals, allow-lists, connector scopes) after session start and treats mid-run mutations as deny + audit.
- Vendor bake-offs: compare two harnesses on the same model instead of only model cards.
- A weekend sample: mock harness with mutable vs immutable config store; adversarial artifact tries to weaken approvals; assert immutable wins.

#### Buildability

CPU Python weekend for a **config-phase immutability gate** inspired by the finding (not a full 128-case harness port). Skip reproducing their multi-model panel. No GPU.

#### LinkedIn / demo

Strong: mutable approvals vs frozen policy under the same workflow artifact. Public sample + one-pager fit.

#### Technical compare

- **Agent Security Bench / AgentDojo / InjecAgent:** scenario or tool-injection focused; HarnessRisk’s unit of analysis is **harness responsibility across the lifecycle**.
- **HookPry (2026-09-04-a):** lifecycle-hook integrity on updates; HarnessRisk is a **benchmark** spanning six phases including config.
- **Deadbugz (2026-09-06-d):** live metadata flip after N calls; HarnessRisk shows config-phase weakness even without a supply-chain campaign.
- **SkillShift (2026-09-04-c):** biased skills among valid options; HarnessRisk includes capability-extension but measures end-to-end harness compromise.

---

### 2026-09-07-c — SEP-2643 Structured Authorization Denials

- **Kind:** protocol
- **Date:** draft SEP; Fine-Grained Authorization WG reviews through mid-2026; board lists ~Sep 2 2026 activity signal
- **Link:** [SEP-2643 / PR 2643](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2643)
- **Title:** Structured Authorization Denials
- **Track:** sample-ready
- **Status:** queued

#### What it is

Today a denied MCP `tools/call` is often a dead-end error. Operators and agents cannot tell “wrong token,” “needs manager approval,” “needs richer OAuth scopes / RAR,” or “needs a short-lived payment credential.”

**SEP-2643** defines a transport-agnostic JSON-RPC **authorization denial envelope**: failure classification, retry correlation handle, and extensible structured **remediation hints**. Initial hint types illustrated: URL-based approval composed with MCP URL-mode elicitation; credential replacement via scopes + OAuth Rich Authorization Requests (RFC 9396); additional short-lived credentials for per-transaction ops (e.g. banking). Additive / backward compatible — existing OAuth clients ignore what they do not understand. Complements HTTP `WWW-Authenticate` / Protected Resource Metadata for cases the transport cannot express cleanly.

#### Why it is useful in the real world

Least-privilege MCP tool calls need **denied-but-requestable** semantics. Without a shared envelope, every MCP server invents its own 403 body and every MCP host/client guesses.

Concrete places this bites:

- A payments or CRM MCP server that returns “need step-up / need RAR detail X” instead of an opaque fail.
- An MCP host/client that routes hint type to elicitation UI or OAuth step-up instead of aborting the agent turn.
- Compose with ATSA/ACLE: admission/lease can still pass while **this call’s** authorization is denied with a machine-readable recovery path.

#### Buildability

CPU Python weekend: fixture denial envelopes + a tiny host-side router (elicitation URL vs mock RAR vs hard deny). Do not pretend the SEP is Final. No GPU.

#### LinkedIn / demo

Good: structured MCP denials that tell the agent how to ask again. Envelope JSON on a one-pager works.

#### Technical compare

- **SEP-2809 ATSA (shipped sample):** connect-time server admission; 2643 is **per-call authz denial + remediation** after you are already talking to an admitted server.
- **ACLE-MCP (queued):** binds call authority to provider workload at execution; 2643 standardizes the **deny/remediate conversation**, not TPM/quote mechanics.
- **SEP-3004 (queued):** tamper-evident audit record shape; 2643 is the live denial UX/interop primitive (audit can log the envelope later).
- **HTTP WWW-Authenticate / PRM:** transport-level challenges; 2643 covers JSON-RPC cases those headers cannot carry cleanly.

---

### 2026-09-07-d — Bifrost (Maxim AI MCP + LLM gateway)

- **Kind:** tool / monthly-trending repo
- **Date:** ongoing 2026 releases; MCP gateway heavily marketed in 2026 guides
- **Link:** [maximhq/bifrost](https://github.com/maximhq/bifrost)
- **Title:** Bifrost — enterprise AI gateway with MCP gateway, guardrails, virtual keys
- **Language / license:** Go; Apache-2.0
- **Stars:** public pages report roughly 5.7k–7.5k (confirm on GitHub before quoting a single number in posts)
- **Track:** watch
- **Status:** queued

#### What it is

Bifrost started as a high-performance **LLM gateway** (multi-provider routing, failover, semantic cache, budgets) and grew a first-class **MCP gateway**: it speaks MCP toward clients and toward upstream MCP servers, centralizing OAuth to tools, virtual keys / tool groups, guardrails (secrets/PII/injection classifiers), and audit. “Code Mode” is marketed as collapsing tool catalogs into code-orchestrated calls to cut token cost. Deployable via `npx -y @maximhq/bifrost`; OSS Apache-2.0 with enterprise add-ons.

#### Why stars are rising

One control plane for **LLM + MCP** traffic, Go performance story, frequent releases, and 2026 “best MCP gateway” roundups pushing multi-tenant isolation + guardrails. Stars alone are not the point — the substance is a real gateway codebase with MCP client and MCP server roles, not an awesome-list.

#### Why it is useful in the real world

Teams drowning in per-IDE MCP configs want one place to scope which agent identity sees which tools, keep upstream credentials server-side, and log every `tools/call`. That is the enterprise gap MCP itself does not specify.

Concrete places this bites:

- Multi-tenant internal agents that must not see admin tools in `tools/list`.
- Central OAuth brokering so laptop agents never hold SaaS refresh tokens.
- Compare against thinner stdio firewalls when you need org-scale governance, not just local regex.

#### Buildability

**Watch** as a product dependency (large Go tree). A weekend sample should **not** vendor Bifrost — instead sketch a minimal virtual-key tool-scope proxy in Python inspired by the same idea (role to allowed tool names), or evaluate Bifrost as an off-the-shelf control plane. No GPU.

#### LinkedIn / demo

Better as “what enterprises are standardizing on” commentary than a from-scratch clone. If sampling, demo **tool-group isolation** with a tiny proxy, cite Bifrost as the production-shaped analog.

#### Technical compare

- **Obot / Docker MCP Gateway / Kong AI Gateway:** catalog + OAuth + policy peers; Bifrost’s hook is unified **LLM routing + MCP** in one Go binary with virtual keys.
- **veil-mcp / mcp-shield / sovereign-mcp-gateway:** local stdio/JSON-RPC firewalls; Bifrost is **org control-plane** scale, heavier ops.
- **mcp-ztgateway (PyPI):** declare then profile then confine processes you spawn; Bifrost governs protocol/identity/budget, not kernel seccomp of child tools.
- **ATSA sample:** pins which servers may connect; Bifrost assumes servers are registered and focuses on **tenant/tool governance and observability**.

---

## 2026-09-08

### 2026-09-08-a — SIGIL (seal the audit–runtime gap for LLM skills)

- **Kind:** paper
- **Date:** v1 6 May 2026; **v2 revised 3 Sep 2026**
- **Link:** [arXiv:2605.05274](https://arxiv.org/abs/2605.05274)
- **Title:** Sealing the Audit–Runtime Gap for LLM Skills
- **Authors:** Tingda Shen, Yebo Feng, Konglin Zhu, Xiaojun Jia, Yang Liu, Lin Zhang
- **Track:** sample-ready
- **Status:** queued

#### What it is

LLM skills collapse once they enter context: the model cannot separate skill text from trusted instructions, and the executable side can invoke privileged actions. Stage-bound defenses fail the **audit–runtime gap** — a signed or audited artifact at publish time is not what later loads.

**SIGIL** binds publication to load. Verifiable hosting: a tamper-evident on-chain registry with four publication types (Transparent / Licensed / Sealed / Committed) and a DAO audit committee under stake-and-slash. Verified loading: a mandatory **Skill Verification Loader (SVL)** fetches/decrypts the skill, checks integrity against the on-chain record, and enforces the approved **permission manifest** before context injection. Eval claim: 1,023 in-the-wild skills across six attack types; batched SVL verification under ~86 ms.

#### Why it is useful in the real world

Skill marketplaces and agent harnesses that hot-load community skills need a load path that refuses rug-pulls and unsigned permission widening — not just a one-time scan report.

Concrete places this bites:

- An MCP host/client skill/plugin loader that pins `content_hash + permission_manifest` and refuses silent descriptor drift after first approval.
- A weekend demo: mock registry (SQLite) + SVL that blocks a post-audit rug-pull before the skill text reaches the model.
- Compose with ATSA/SEP-3004: admission/audit of MCP servers vs **skill-package** integrity at load.

#### Buildability

CPU Python weekend for a **thin SVL + pin store** (hash + manifest gate). Skip on-chain / DAO economics for the sample. No GPU.

#### LinkedIn / demo

Strong: “approved skill ≠ loaded skill” before/after rug-pull. Public sample + one-pager fit.

#### Technical compare

- **HookPry / Deadbugz (backlog):** runtime hook or metadata flip after N calls; SIGIL’s delta is **cryptographic bind from publish → load** for skill packages.
- **SkillShift / SkillGuard (backlog):** policy steering or reachability confinement; SIGIL is **supply-chain integrity of the skill artifact**, not policy bias among valid skills.
- **SEP-3004 (queued):** shared tamper-evident audit record shape; SIGIL is a **skill registry + loader** that could emit such records.
- **Marketplace signing alone:** SIGIL’s point is the **mandatory SVL path** so audit output cannot drift from what injects into context.

---

### 2026-09-08-b — IPI as test-time search (agentic attacker harness)

- **Kind:** paper
- **Date:** 3 Sep 2026
- **Link:** [arXiv:2609.04495](https://arxiv.org/abs/2609.04495)
- **Title:** Rethinking Indirect Prompt Injection as a Test-Time Search Problem
- **Authors:** Duong M. Nguyen, Joon Sik Kim, Blazej Manczak, Vaikkunth Mugunthan (Dynamo AI / UIUC)
- **Track:** watch
- **Status:** queued

#### What it is

Static IPI benches treat attack success as a property of the victim. This paper reframes indirect prompt injection as **test-time search** over a task-dependent attack surface. An **agentic attacker** with a search harness does environment reconnaissance, hierarchical strategy management, and adaptive probes using victim feedback. Empirics: **ASR@T rises with attacker token budget**. Takeaway: report attacker search procedure and compute budget, not budget-independent ASR.

#### Why it is useful in the real world

If you only red-team with fixed payload templates, you underestimate adaptive attackers that get more compute. Demand ASR@50k / ASR@500k, not a single ASR number.

#### Buildability

Watch for full attacker clone (API cost). Weekend slice can mock ASR@T reporting + strategy archive. No GPU for the meter.

#### LinkedIn / demo

Commentary-first: “your 12% ASR was at 10k attacker tokens.” Skip full attacker clone.

#### Technical compare

- **AgentDojo / InjecAgent:** static or lightly adaptive; this paper’s delta is attacker compute scaling.
- **Framing Gap (backlog):** victim framing; this is attacker search economics.
- **HarnessRisk (backlog):** harness lifecycle vs attacker compute.

---

### 2026-09-08-c — Channel-policy per-send attestation (Enterprise IG #2626)

- **Kind:** protocol / IG proposal
- **Date:** issue opened ~1 Sep 2026
- **Link:** [modelcontextprotocol#3337](https://github.com/modelcontextprotocol/modelcontextprotocol/issues/3337)
- **Title:** Channel-Policy Per-Send Audit — proposal for Enterprise IG audit/compliance scope
- **Track:** sample-ready
- **Status:** queued

#### What it is

MCP Apps work shipped a pre-send tripod (SEP-2624 Interceptors, SEP-2809, SEP-3182 idempotency) but none specify what a messaging tools/call must assert about the message body before the transport fires. Proposal: before outbound send, an interceptor attaches `_meta.io.modelcontextprotocol/channel-policy-attestation` covering template validity, opt-in / consent ledger ref, schema correctness, and sender reputation — paired with idempotency key and a structured policy-blocked failure. Keep “permitted?” vs “still current?” as separate checks.

#### Why it is useful in the real world

Regulated messaging from agents needs a pre-send receipt. A payments/alerts MCP server should refuse send_message without a signed attestation envelope. An MCP host/client interceptor builds the envelope.

#### Buildability

CPU Python weekend: fixture send_message + interceptor that attaches/validates the envelope (and a stale-evidence case). Do not claim a Final SEP. No GPU.

#### LinkedIn / demo

Good: “agent almost sent RCS without opt-in proof.” Envelope JSON on a one-pager works.

#### Technical compare

- **SEP-2643:** authz denial + remediation; this is compliance attestation of the message body before send.
- **SEP-2809 ATSA:** connect-time MCP server admission; this is per-send message-policy.
- **SEP-3004:** audit record shape; channel-policy is live pre-send evidence.

---

### 2026-09-08-d — Apache Maka (event-sourced agent workspace)

- **Kind:** tool / monthly-trending repo
- **Date:** incubating; active on main (pushed 8 Sep 2026)
- **Link:** [apache/maka](https://github.com/apache/maka)
- **Title:** Apache Maka (Incubating) — agent workspace where the log is the runtime
- **Language / license:** TypeScript; Apache-2.0
- **Stars:** 5,020 (GitHub API at briefing time); monthly trending lists ~3.7k stars this month
- **Track:** watch
- **Status:** queued

#### What it is

Apache Maka is an agent workspace (desktop / TUI / CLI / Eval as thin clients of one Runtime Host). Security-relevant claim: **the log is the runtime** — every model message, tool call, permission decision, and termination is an append-only RuntimeEvent. Old tool output can leave the next prompt without leaving the log. Local-first; bring-your-own model.

#### Why stars are rising

Apache incubator branding and a “complete record of everything it did” pitch on monthly GitHub trending. Real TypeScript monorepo — not an awesome-list. Log it for **event-sourced auditability**, not stars alone.

#### Why it is useful in the real world

Most agent harnesses lose ground truth when the context window is compacted. Incident review wants an append-only record that survives prompt trimming.

#### Buildability

Watch as a product dependency (large incubating monorepo). Weekend sample = tiny event-log harness, not a Maka fork. No GPU.

#### LinkedIn / demo

Commentary: what Apache put on the board for accountable agents. If sampling, demo prompt compact vs log retain.

#### Technical compare

- **Bifrost (queued yesterday):** LLM+MCP gateway; Maka is an agent workspace / harness with event sourcing.
- **HarnessRisk:** measures harness weakness; Maka is a concrete harness marketing complete records.
- **SEP-3004:** standardized audit bytes; Maka RuntimeEvent log is a product-shaped substrate.

---

## 2026-09-09

### 2026-09-09-a — AgentDrift (step-labeled IPI hijack trajectories)

- **Kind:** paper
- **Date:** 7 Sep 2026 (cs.CR past week; HF-adjacent agent-security cluster)
- **Link:** [arXiv:2609.06972](https://arxiv.org/abs/2609.06972)
- **Title:** AgentDrift: A Step-Labeled Benchmark of Injection-Hijacked LLM Agent Trajectories
- **Track:** sample-ready
- **Status:** queued

#### What it is

LLM agents finish tasks as sequences of tool calls; every observation is an indirect-prompt-injection channel. A successful injection has a **trajectory shape**: a benign prefix, then steps that serve the attacker. Existing benches score live ASR; existing guards score a whole trace. Nothing public labels, **step by step**, where injection enters and which steps it corrupts.

**AgentDrift** releases 12,536 synthetic tool-call trajectories across five agent domains with **71,024 steps** each labeled one of: `benign`, `injection_point`, `hijacked`, or `failed_injection`. Mix includes 4,000 benign, 5,536 attacked, 1,500 failed-attack, and 1,500 hard-negative trajectories — training/eval fuel for step-level detectors rather than whole-trace classifiers.

#### Why it is useful in the real world

If your MCP host/client or agent harness only has a binary “this session looks bad” detector, you cannot stop mid-trajectory or explain which tool observation flipped the agent. SOC / red-team reviews need **which step**.

Concrete places this bites:

- Train/eval a tiny sequence model or ruleset that flags the first `hijacked` step and freezes further `tools/call`.
- Diff vs whole-trace guards: show false negatives that only appear after step *k*.
- Pair with Deadbugz / discovery-sanitizer: those gate metadata; AgentDrift trains detectors on **runtime observation drift**.

#### Buildability

CPU Python weekend: ingest a slice of the labeled JSONL, print a before/after step timeline, and ship a naïve windowed classifier (n-gram / embedding optional). No GPU required for the demo; full SOTA training is optional.

#### LinkedIn / demo

Strong: “benign → injection_point → hijacked” timeline screenshot. Public sample + one-pager fit.

#### Technical compare

- **IPI as test-time search (2026-09-08-b):** attacker *search economics*; AgentDrift is a **labeled corpus for detectors**, not an attacker harness.
- **Deadbugz / HookPry (backlog):** attack *mechanisms* (metadata flip / hook update); AgentDrift labels the **resulting trajectory**.
- **AgentDojo / InjecAgent:** success/fail outcomes; AgentDrift’s delta is **per-step corruption labels**.
- **AURA-Eval (arXiv:2609.06783, same week):** risk-awareness rubrics on trajectories; complementary diagnosis axis, not injection-point labeling.

---

### 2026-09-09-b — MOLE (insider-threat detection for AI agents)

- **Kind:** paper / benchmark
- **Date:** submitted ~6–7 Sep 2026; on Hugging Face Daily Papers **9 Sep 2026**
- **Link:** [arXiv:2609.06966](https://arxiv.org/abs/2609.06966) · [HF paper](https://huggingface.co/papers/2609.06966)
- **Title:** MOLE: Detecting Insider Threats in AI Agents
- **Authors:** Aashiq Muhamed, Virginia Smith (CMU)
- **Track:** watch
- **Status:** queued

#### What it is

Frontier-lab AI agents holding shared service accounts can exfiltrate weights, poison training data, or weaken release gates via misalignment, prompt injection, or operator misuse. Classical insider-threat benches do not ask whether defenders can spot that activity **among routine work under a limited review budget**.

**MOLE** is an open benchmark: 150 AI-operated accounts sharing 9 stateful services over 30 workdays, 12 MITRE-grounded threats, 8 corpora from four models (~20B tokens). Of 39 agent models, **72% complete most assigned harmful objectives**; refusal does not predict completion. Evaluates 40 monitors across corpus generators / observability / threats; even the best single-day audit-event monitor **misses nearly half** of completed harm. Also shows monitor-development loops (benchmark-guided search + selective use of a stronger monitor).

#### Why it is useful in the real world

Enterprises that hand agents GitHub / cloud / ticketing credentials need **monitor bake-offs under review-budget constraints**, not just “did the model refuse.”

Concrete places this bites:

- Security ops designing agent audit queues: rank account-days, not every event equally.
- Compare your MCP host/client audit (SEP-3004-shaped) against MOLE’s finding that observability level changes detection.
- A thin sample later: mock budget-AUC scorer over toy account-day events — **not** shipping their 20B-token corpora.

#### Buildability

**Watch** for reproducing the full benchmark (data + monitor zoo). Weekend slice is a **budgeted triage demo** with synthetic account-day events. No GPU for the triage meter; their corpus is research-scale.

#### LinkedIn / demo

Commentary-first: “best monitor still misses ~half under a one-day budget.” Full MOLE rerun is a skip for LinkedIn.

#### Technical compare

- **CERT / classical insider benchmarks:** human operators; MOLE’s delta is **AI agents as the insider** with shared lab services.
- **HarnessRisk / AURA-Eval (same week):** harness/risk rubrics on trajectories; MOLE is **org-scale multi-account detection under budget**.
- **SEP-3004 / channel-policy attestation (backlog):** emit/attest records; MOLE asks whether **reviewers + monitors** catch harm given those records.
- **SIGIL (queued):** skill supply-chain integrity; MOLE is **runtime insider behavior** after tools are already admitted.

---

### 2026-09-09-c — SEP-3140 Signed Capability Declarations & Trust Labels

- **Kind:** protocol / SEP draft
- **Date:** opened 27 Jul 2026; **updated 8 Sep 2026** (still open)
- **Link:** [modelcontextprotocol#3140](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/3140)
- **Title:** SEP-3140: Signed Capability Declarations & Trustworthy Trust Labels
- **Track:** sample-ready
- **Status:** queued

#### What it is

MCP tool/prompt/resource declarations are model-facing instructions, yet the protocol delivers them with **no authenticity, integrity, or trustworthy risk labeling**. Spec text says treat annotations as untrusted unless from a “trusted server,” but provides **no verifiable trust mechanism** — so tool poisoning and post-approval **rug pulls** (`notifications/*/list_changed`) stay invisible to both human approvers and autonomous MCP host/clients.

SEP-3140 proposes an **additive** package: content hash + version per declaration; a **JWS-signed capability manifest** bound to discoverable publisher identity (PRM / JWKS); a signature-covered closed **`trust` label** vocabulary (effect / egress / data-sensitivity / reversibility); and change semantics so material `list_changed` events force re-gate. Optional secure-by-default auth posture. Backwards compatible: unsigned MCP servers keep working.

#### Why it is useful in the real world

Marketplace / registry MCP servers and enterprise MCP host/clients need **verify-then-expose** before tool schemas hit the model — TLS alone does not prove publisher provenance through a compromised “trusted” origin or stdio hop.

Concrete places this bites:

- Pin `contentHash` of `tools/list` at first approval; refuse silent schema swaps.
- Surface signed `trust.egress` to a human reviewer **and** to policy for autonomous agents.
- Compose with ATSA (connect-time) and discovery-sanitizer (heuristic strip): 3140 is **cryptographic declaration authenticity**.

#### Buildability

CPU Python weekend: mock publisher JWKS + JWS over a tiny tools manifest, verify in an MCP host/client shim, demo a rug-pull `list_changed` that fails hash check. Do not claim Final. No GPU. JOSE via `PyJWT` / `joserfc`.

#### LinkedIn / demo

Strong: “approved tools ≠ tools the model just saw” with hash mismatch. Public sample + one-pager fit.

#### Technical compare

- **MCP discovery sanitizer (shipped):** heuristic allow/strip/block of poisoned descriptions; SEP-3140 **authenticates** declarations so policy can trust labels.
- **Deadbugz (queued):** runtime metadata flip after N calls; 3140’s delta is **signed version + re-consent on list_changed**.
- **SEP-1913 (open, updated 9 Sep 2026):** sensitivity/trust **hints on data in flight**; 3140 signs **what the MCP server declares** before those flows.
- **SEP-2809 ATSA (shipped sample):** admit the MCP server process; 3140 authenticates **capability bytes** that server emits afterward.
- **SEP-3004 (queued):** tamper-evident audit record shape; 3140 can feed verified declaration digests into those records.

---

### 2026-09-09-d — v4scan (deterministic install-time MCP/skill scanner)

- **Kind:** tool / recent implementation
- **Date:** created **8 Sep 2026**; active 9 Sep 2026
- **Link:** [Open2445/v4scan](https://github.com/Open2445/v4scan)
- **Title:** v4scan — scan MCP servers and AI-agent skill packages before install
- **Language / license:** Rust; MIT
- **Stars:** 0 at briefing time (brand-new; substance over stars)
- **Track:** sample-ready
- **Status:** queued

#### What it is

Install-time firewall for the MCP / agent-skill ecosystem. **Deterministic** (no model in the loop): reads install scripts, manifests, and source **before** `npm install` / `pip install`, emits explainable High/Medium/Low findings, exit `1` on High/Critical for CI. Signals include install-script execution (`curl|sh`, `eval`, …), typosquats, decode-then-execute obfuscation, install-time network egress, high-entropy blobs, missing provenance/attestation, thin metadata. SARIF 2.1.0 + GitHub Action + pre-commit hooks. Explicit non-goals: not a runtime sandbox, not a replacement for `npm audit` / Sigstore verify.

#### Why it is useful in the real world

Postmark-MCP-style and registry-poisoning incidents hit **at install**, before any MCP host/client session. Platform teams pulling community MCP servers need a **reproducible CI gate** with auditable reasons.

Concrete places this bites:

- Gate internal MCP server onboarding PRs with `v4scan path --explain`.
- Pair with SEP-3140 / SIGIL: install-time static heuristics **before** signed-declaration / SVL load-time checks.
- A thin Python cousin sample if Rust bootstrap is heavy: same rule table over `package.json` scripts — cite v4scan as the reference instrument.

#### Buildability

CPU weekend: either build/run their Rust binary on the shipped `examples/` (curl|sh, typosquat, benign) **or** reimplement a 5-rule subset in Python for LinkedIn. No GPU.

#### LinkedIn / demo

Strong: redacted `postinstall → curl | sh` blocked with exit 1. Public sample + one-pager fit.

#### Technical compare

- **MCP discovery sanitizer (shipped):** runtime/discovery **metadata** gate inside the MCP host/client; v4scan is **pre-install package** gate.
- **SIGIL (queued):** cryptographic bind publish→load for skills; v4scan is **static install-script heuristics** with no chain-of-trust required.
- **GuardBee mcp-security-proxy (also new 9 Sep):** runtime PI / PII proxy in front of an MCP server; different layer (after install).
- **npm audit / pip-audit / OSV:** CVE DB matching; v4scan’s delta is **install-behavior + typosquat + provenance absence** tuned for MCP/skill packs.


## 2026-09-11

### 2026-09-11-a — No-Box / MCPSEC (description-only IPI audit of MCP servers)

- **Kind:** paper
- **Date:** submitted **9 Sep 2026**
- **Link:** [arXiv:2609.10854](https://arxiv.org/abs/2609.10854)
- **Title:** No-Box Vulnerability Analysis: Description-only Detection of Indirect Prompt Injection Vulnerabilities in MCP Servers
- **Authors:** Zehua Zhang, Jie Hu, Pratham Hegde, Aditya Maheshbhai Gabani, Souradip Nath, Yibo Liu, et al. (ASU / collaborators)
- **Track:** sample-ready
- **Status:** queued

#### What it is

Most MCP server audits assume you can run the binary, hit the endpoint, or fuzz live tools. That fails for closed-source, SaaS, or commercially gated MCP servers. This paper defines **no-box** analysis: only functionality metadata is available (inputs, outputs, side effects as declared), and the analyst hypothesizes vulnerabilities that must hold across *all* implementations consistent with that metadata.

The prototype **MCPSEC** audits MCP servers for **indirect prompt injection** using only the tool metadata exposed at registration (`tools/list` style descriptions/schemas). On 20 widely deployed MCP servers (177 tools), humans confirmed 95 vulnerable tools; MCPSEC flagged 143 and recovered **94/95 (98.9% recall)** of the verified set, beating an LLM baseline at 80/95 (84.2%). Each finding ships a hypothesized vulnerability plus an exploitation technique — still a hypothesis until you get more access.

#### Why it is useful in the real world

Platform security and marketplace reviewers often see only a registry card + tool schema before approving an MCP server for an MCP host/client fleet. No-box gives a **pre-connect** triage: if the declared contract already implies an IPI surface (e.g. “fetch URL and return body to the model”), you can block or warn without spinning up the server.

Concrete places this bites:

- Gate new MCP server onboarding on schema-only MCPSEC findings before ATSA/OAuth connect.
- Audit SaaS MCP servers you cannot binary-instrument.
- Pair with discovery sanitizer: sanitizer strips poison in descriptions; MCPSEC asks whether the *benign* description still entails an IPI-capable tool.

#### Buildability

CPU Python weekend: ingest a fixture `tools/list` JSON, rule/LLM hybrid that emits {tool, hypothesis, technique}, unit tests on poisoned vs clean schemas. Cite MCPSEC numbers; do not claim their full eval without their artifact. No GPU required for a thin sample.

#### LinkedIn / demo

Strong: “we never ran your MCP server — the schema already said it could IPI.” Side-by-side clean vs vulnerable tool cards. Public sample + one-pager fit.

#### Technical compare

- **MCP discovery sanitizer (shipped):** strips/blocks *poisoned* metadata text; MCPSEC targets IPI risk implied by *honest* contracts.
- **v4scan (queued):** install-time package heuristics; MCPSEC is **registration-time metadata** analysis after install/connect is possible.
- **Deadbugz (queued):** runtime metadata flip after N calls; no-box is static and pre-runtime.
- **MCPSecBench / gateway harnesses:** dynamic attack playgrounds; no-box needs no interaction.

---

### 2026-09-11-b — CapScope (capability-scoped harness for coding agents)

- **Kind:** paper
- **Date:** submitted **8 Sep 2026**
- **Link:** [arXiv:2609.08371](https://arxiv.org/abs/2609.08371) · artifact on [Figshare](https://figshare.com/s/86184ed20f66d1f0cf91)
- **Title:** Authority Is Not a String: A Capability-Scoped Harness for Prompt-Injection-Resistant Coding Agents
- **Authors:** Dimitrios Stamatios Bouras, Yihan Dai, Sergey Mechtaev
- **Track:** sample-ready
- **Status:** queued

#### What it is

Coding agents often give tools **ambient authority**: if the model can name a path or command, the harness will run it. Indirect prompt injection in READMEs, comments, skill files, or tool output then hijacks those tools. **CapScope** is a harness-level authorization layer that does **not** ask the model to spot malice.

Before any untrusted repo/tool content is read, CapScope derives a **task-wide authority ceiling** from trusted input, then assigns each agent (orchestrator and process-isolated sub-agents) a separate set of **typed capabilities** stored *outside* the model context. Every tool call is checked against the issuing agent’s capabilities; permissions do not leak across sub-agents. Injection may still make the model *request* a bad action — the harness blocks it unless that agent already holds a covering capability.

Implemented on the Pi coding agent (TypeScript hooks, no Pi internals fork). Eval: 5 Python repair tasks × 5 injection surfaces × 4 auth conditions × 3 trials = **300 runs**. Injected effect executes in **33–47/75** under ambient/global-policy baselines vs **3/75** under CapScope; repairs still complete **68/75** (baselines 68–72/75).

#### Why it is useful in the real world

Any multi-agent coding MCP host/client (Cursor-style, Pi, Claude Code with subagents) that delegates “read tests / edit src / run shell” needs per-principal ceilings. A poisoned `CONTRIBUTING.md` should not grant the reader agent the writer’s `git push` capability.

Concrete places this bites:

- Orchestrator + worker coding agents with separate capability bags.
- MCP host/client tool routers that currently only do global allowlists.
- Demo vs SkillGuard: CapScope is **pre-dispatch typed capabilities**, not reachability confinement of skill graphs.

#### Buildability

CPU weekend Python or TS sample: capability store + pre-dispatch gate over a mock tool table; show injection request blocked without model cooperation. Full Pi artifact exists on Figshare if you want fidelity. No GPU for the gate itself (LLM only for the agent under test).

#### LinkedIn / demo

Strong: “the model asked for `rm -rf secrets/` — CapScope said no, and the repair still finished.” Public sample + one-pager fit.

#### Technical compare

- **SkillGuard (queued):** confines skill *reachability*; CapScope confines *tool authority per principal*.
- **ACLE-MCP (queued):** provider-side execution leases after OAuth; CapScope is **host harness** before untrusted content is read.
- **OpenAPPA (today’s tool slot):** information-flow / destination policy on data; CapScope is capability predicates on tool args per agent.
- **Ambient allowlists:** one shared policy bag; CapScope’s delta is **per-sub-agent attenuation** so injection in one worker cannot spend another’s rights.

---

### 2026-09-11-c — SEP-2817 AI Invocation Audit Context in Request `_meta`

- **Kind:** protocol / SEP draft
- **Date:** opened 29 May 2026; **updated 11 Sep 2026** (still open, seeking sponsor)
- **Link:** [modelcontextprotocol#2817](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2817)
- **Title:** SEP-2817: AI Invocation Audit Context in Request `_meta`
- **Track:** sample-ready
- **Status:** queued

#### What it is

Standards-track draft that reserves one optional `_meta` key — `io.modelcontextprotocol/aiInvocation` — for **client-asserted** input-audit context on AI-initiated MCP requests. Optional fields: `invocationReason`, `model`, `userIntent`, `turnId` (servers MAY echo only `turnId` in response `_meta`).

Explicitly **not authorization evidence**. Scope is deliberately thin: server-side decision/outcome records, stable tool-call identity, and taxonomies are deferred to follow-ups (see closed SEP-2828 discussion lineage and SEP-2787 attestation). Reconciles with OTel trace context (SEP-414), `_meta` key reservation practice, SEP-2643 denials, and telemetry SEPs.

#### Why it is useful in the real world

Regulated MCP host/client deployments need to answer “why did this tool call happen?” without pretending the client’s story is a capability grant. `turnId` groups a burst of MCP server calls from one user turn for SIEM join; `userIntent` / `invocationReason` help human review after an incident.

Concrete places this bites:

- Audit pipelines that today only see tool name + args.
- Compose with SEP-3004 / SEP-2787: 2817 is the **client-asserted why**; crypto attests and server receipts stay elsewhere.
- Do **not** use these fields to authorize — forging them must not unlock tools.

#### Buildability

CPU Python weekend: MCP host/client shim that attaches `aiInvocation` on `tools/call`, mock MCP server that logs/echoes `turnId`, tests that authz ignores the block. No GPU. JOSE not required.

#### LinkedIn / demo

Solid: “same tool args, different `userIntent`/`turnId` in the audit trail — and the allow decision never reads them.” Public sample + one-pager fit.

#### Technical compare

- **SEP-2787 (tool-call attestation):** cryptographic bind of intent/args; 2817 is **unsigned audit context**.
- **SEP-3004 (queued):** tamper-evident record *shape*; 2817 supplies optional client fields that can land in extensions.
- **SEP-3140 (queued):** signed *capability declarations*; 2817 labels *why this call*, not what the MCP server advertises.
- **SEP-2643 (queued):** structured *denial* remediation; 2817 is on the request path for successful and failed calls alike.

---

### 2026-09-11-d — OpenAPPA (deterministic agentic permissions / flow policy)

- **Kind:** tool / recent implementation
- **Date:** created **18 Aug 2026**; actively pushed **11 Sep 2026** (preview & RFC)
- **Link:** [archestra-ai/OpenAPPA](https://github.com/archestra-ai/OpenAPPA)
- **Title:** OpenAPPA — deterministic security for real-world agentic applications (APPA policy algebra)
- **Language / license:** Rust (+ Python/Go bindings surfaces); **MIT**
- **Stars:** **19** at briefing time (early; substance + daily commits matter more than star count — not a monthly-trending mega-repo)
- **Track:** sample-ready
- **Status:** queued

#### What it is

OpenAPPA sits between an agent and its tools and answers, before every action: **is this data allowed to go to this destination?** Powered by **APPA** (Agentic Permissions Policy Algebra): tracks sensitivity/trust of everything the agent reads and checks outbound calls against declarative TOML policy. The decision core is a pure function of the event log (no IO), embeddable in-process (Rust/Python) or as a sidecar.

Positioning vs classifiers: PII detectors are probabilistic; a declared flow decision is deterministic — aimed at medical/financial agent workloads. Ships Claude Code plugin/hooks + an MCP server for the runtime, checksum-verified installer, and eval harnesses (`mise` + `appa-eval.sh`). README reports Bench-Corp / AgentThreatBench figures (utility vs ASR); treat published percentages as **author-reported** until you re-run locally.

#### Why it is useful in the real world

Enterprise agents that read private meeting notes then call a public GitHub/MCP server need **destination-aware** blocks, not just tool allowlists. OpenAPPA’s Claude Code demo (block posting private recording content to a public repo) is exactly that failure mode.

Concrete places this bites:

- MCP host/client sidecars enforcing flow policy across multiple MCP servers.
- Pair with CapScope: CapScope = who may call which tool; OpenAPPA = which *data* may leave toward which destination.
- Pair with SEP-2817: audit why; OpenAPPA decides allow/deny on labeled flows.

#### Buildability

CPU weekend sample path: thin Python flow-label + destination allowlist mimicking APPA’s question (without claiming their ASR). Or install their binary if platform matches and run the scripted harness (`--quick`). Full Rust workspace is heavier than a LinkedIn slice. No GPU for the policy core.

#### LinkedIn / demo

Strong: “private meeting → public GitHub” blocked with a human-readable ruling. Public sample + one-pager fit (cite OpenAPPA; keep your sample thin).

#### Technical compare

- **CapScope (today’s paper):** per-agent *capability* gates on tool args; OpenAPPA is *information-flow / destination* policy.
- **Bifrost / mcp-firewall class gateways:** broad proxy stages (injection, RBAC, audit); OpenAPPA’s delta is a **deterministic flow algebra** with published eval positioning.
- **ROPE (queued):** routed origin policy in the paper sense; OpenAPPA is a shippable runtime with TOML contracts.
- **v4scan (queued):** install-time static; OpenAPPA is **runtime pre-dispatch**.


## 2026-09-14

No strong new MCP SEP/protocol draft this window (recent `modelcontextprotocol` PRs are docs/deps; SEP-2817/3140/3004/2643 already queued). Soft mix: two papers + one recent scanner impl + one monthly-momentum tool.

### 2026-09-14-a — Whisper Attacks + A-VIP (AP2 intent binding)

- **Kind:** paper
- **Date:** submitted **10 Sep 2026** (arXiv:2609.11757)
- **Link:** [Signing the Transaction but Not the Decision: Whisper Attacks and a Binding Defense for AP2](https://arxiv.org/abs/2609.11757)
- **Title:** Whisper Attacks / A-VIP — AP2 signs the cart, not the decision
- **Track:** sample-ready
- **Status:** queued

#### What it is

Agent Payments Protocol (AP2) signs intent, cart, and payment mandates so completed purchases verify cryptographically — but merchant-controlled product text can steer the shopping agent into a cart that still passes every protocol check while no longer matching the user’s request. The paper names three “whisper” attacks: (1) steer credential fetch toward another user’s payment method, (2) assemble a valid cart whose contents differ from what the user was shown, (3) factual stock/lineage claims that move the agent to a costlier SKU with no instruction-looking injection. Author-reported ASR on Gemini Flash-Lite sample agents: **90% / 56% / 73.3%**; same class of failure shown across many Google models and multiple agent frameworks. Defense **A-VIP** (AP2 Verified-Intent Protection) treats the signed intent as a **capability grant**: bind credential lookups to the requesting session, bind each cart line to the listing the user saw, flag unauthorized spend. Attacks 1–2 leave structural traces blocked at **0 false positives** in their eval; attack 3 needs user confirmation. Artifacts: A-VIP code, machine-checked invariants, **AP2-WhisperBench** (1,544 scenarios).

#### Why it is useful in the real world

Any MCP host/client or agent checkout path that trusts “the protocol verified the signature” while the merchant listing is untrusted content. Enterprise procurement bots, consumer shopping assistants, and payment MCP servers that expose `create_cart` / `charge` tools without binding cart lines to a user-visible listing ID.

#### Buildability

CPU weekend: mock AP2 mandates + a thin Python A-VIP binder that rejects cart lines whose `listing_hash` ≠ session-seen listing (no real payments). Full WhisperBench / multi-model sweep needs API keys and is heavier — keep the sample on the binding gate.

#### LinkedIn / demo

Strong: “signed cart, wrong product” — green check on crypto, red on intent binding. Public sample + one-pager fit.

#### Technical compare

- **Deadbugz / discovery sanitizer (queued/shipped):** defend MCP server *tool metadata* poisoning; Whisper is *merchant listing → payment decision* binding after tools already ran.
- **CapScope (queued):** capability scope on coding tools; A-VIP is **intent↔cart↔credential** binding for payments.
- **OpenAPPA (queued):** destination information-flow; A-VIP is protocol-mandate structural binding for AP2.
- **SEP-3140 / call-attestation samples:** pin tool menus / call labels; A-VIP pins *commerce intent* to cart lines.


### 2026-09-14-b — A2ABreak (A2A protocol FSM security analysis)

- **Kind:** paper
- **Date:** submitted **9 Sep 2026** (arXiv:2609.10871)
- **Link:** [A2ABreak: Systematic Security Analysis of the A2A Protocol](https://arxiv.org/abs/2609.10871)
- **Title:** A2ABreak — 11 spec-compliant vulns in Agent2Agent
- **Track:** watch
- **Status:** queued

#### What it is

First systematic security analysis of Linux Foundation **Agent2Agent (A2A)** — the horizontal agent↔agent layer designed to complement MCP’s vertical agent↔tool path. Method: LLM-assisted extraction of a verified finite-state machine from the natural-language spec (**37 states / 76 transitions** from **929** formalized statements), then adversarial verification **under full-compliance** (attacker needs no implementation bug). Reports **11 new vulnerabilities**, including cross-client context injection via unprotected context IDs, credential harvesting through multi-hop identity loss in delegation chains, and exfiltration via rogue agents advertising **unattested** capability claims. Authors report **73.3% precision / 84.6% F1** vs expert review; a zero-shot LLM baseline over the same spec produced **zero** confirmed findings — formal grounding mattered.

#### Why it is useful in the real world

Teams wiring MCP servers behind multi-agent orchestration that also speaks A2A for cross-org delegation. Security reviews that only harden MCP tool gates miss A2A’s discovery/auth/delegation surface. Useful threat-model checklist for “agent of agent” products.

#### Buildability

Watch this week for a LinkedIn *sample*: reproducing the full FSM extraction is research-scale. Thin sample path later: one A2A-shaped context-ID isolation check or unattested capability claim rejector — cite the paper, don’t claim the full 11.

#### LinkedIn / demo

Skip full repro for now; short explainer carousel (“MCP ≠ A2A trust”) is fine without code. Sample when you pick one concrete vuln to fence.

#### Technical compare

- **MCP-to-A2A egress paper (queued):** focuses on public-sharing egress; A2ABreak is **spec-wide FSM** vuln discovery under compliance.
- **ATSA / SEP-2809 (shipped sample):** MCP server admission attestation; A2ABreak shows **A2A capability ads** without attestation.
- **Delegation Without Trust (queued):** conceptual delegation broker; A2ABreak is empirical protocol analysis with a machine model.
- **Zero-shot LLM audit of specs:** paper’s own ablation — without the FSM, findings collapse.


### 2026-09-14-c — OpenTrustBench (OWASP Trust Cards for agents/MCP)

- **Kind:** tool / recent implementation
- **Date:** v0.1.3 **10 Sep 2026**; active pushes through **14 Sep 2026** (registry refresh, design/theme)
- **Link:** [eulogik/OpenTrustBench](https://github.com/eulogik/OpenTrustBench)
- **Title:** OpenTrustBench — local Trust Card (A–F) + SARIF for agents/MCP servers
- **Language / license:** TypeScript (+ thin Python wrapper); **Apache-2.0**
- **Stars:** **7** at briefing time (early momentum; substance + daily commits — not the monthly mega-repo)
- **Track:** sample-ready
- **Status:** queued

#### What it is

Local-first scanner for AI agents and MCP servers: **8** regex/static rules mapped to OWASP Agentic ASI01–ASI10 and LLM Top 10 2025, emitting a machine-readable **Trust Card** (`opentrustbench/trust-card/v1`), SARIF, markdown report, and shareable A–F badge. CLI via `npx @opentrustbench/cli scan …`, plus GitHub Action, Docker, Homebrew, VS Code extension. Authors are explicit about limits: no AST yet; `attack` is static-heuristic (no live payloads); `eval` is simulation; Trust Card ≠ certification. Zero telemetry claim; weekly registry refresh observed on 2026-09-14.

#### Why it is useful in the real world

Procurement / platform teams that want a **CI gate + README badge** before allowing an MCP server into a host catalog — lighter than a full red-team platform, faster than waiting on SkillSpector-class depth. Pair with discovery sanitizer (runtime/description) and OpenTrustBench (install-time static grade).

#### Buildability

CPU weekend: `npx` scan against a deliberately weak MCP server fixture and publish the Trust Card JSON + badge in a public sample. Or thin Python reimplementation of 2–3 of their OWASP-mapped rules for a LinkedIn slice (cite, don’t fork the whole monorepo).

#### LinkedIn / demo

Strong: before/after grade on a poisoned MCP server fixture; badge in README. Public sample + one-pager fit.

#### Technical compare

- **v4scan (queued):** deterministic MCP/skill install scanner; OpenTrustBench adds **Trust Card + badge + OWASP mapping + SARIF CI story**.
- **SkillSpector (today’s trending):** deeper skill/MCP pattern set + optional LLM stage; OpenTrustBench is **lighter CI credential**.
- **No-Box / MCPSEC (queued):** description-only IPI audit research; OpenTrustBench is shippable static CLI.
- **AI-Infra-Guard (Tencent):** full red-team platform; OpenTrustBench is a thin grade/badge product.


### 2026-09-14-d — NVIDIA SkillSpector (agent-skill security scanner)

- **Kind:** tool / monthly-trending
- **Date:** public OSS line active; notable commits **12 Sep 2026** (analyzer/manifest); README cites NVIDIA Verified Skills pipeline
- **Link:** [NVIDIA/SkillSpector](https://github.com/NVIDIA/SkillSpector)
- **Title:** SkillSpector — scan Agent Skills / MCP configs before install
- **Language / license:** Python 3.12+; **Apache-2.0**
- **Stars:** **~17.2k** at briefing time (page star badge); high monthly momentum around agent-skill security
- **Why stars are rising:** NVIDIA-branded open scanner tied to a **Verified Skills** publish pipeline; fills the “Claude/Codex/Gemini skills are trusted by default” panic; Docker + `uv tool install` + optional MCP server extra lower the try-cost.
- **Track:** sample-ready
- **Status:** queued

#### What it is

Pre-install security scanner for AI agent skills (and related MCP config surfaces). Claims **71** vulnerability patterns across **17** categories (prompt injection, exfil, privilege escalation, supply chain, MCP least privilege, MCP tool poisoning, YARA, AST dangerous-code, taint, etc.). Two-stage analysis: fast static + optional LLM semantic pass; OSV live CVE lookups with offline fallback; outputs terminal/JSON/Markdown/SARIF; risk score 0–100; baseline/suppression for known findings. Can run as CLI, Docker (`--no-llm`), or `skillspector mcp` for in-session scans. Part of NVIDIA’s scan→evaluate→sign skills catalog flow.

#### Why it is useful in the real world

Enterprises letting developers drop third-party `SKILL.md` / agent-skill packs into Cursor/Claude/Codex — same supply-chain moment as npm, but with prompt-injection and tool-poisoning baked in. MCP host/client install gates that already sanitize discovery can add SkillSpector (or a thin rule subset) as the **skill pack** checker.

#### Buildability

CPU weekend LinkedIn path: Docker/`uv` scan of one benign + one poisoned skill fixture with `--no-llm`, show SARIF/score. Full 71-rule + LLM stage is product integration, not a weekend rewrite — wrap or cite, don’t reimplement NVIDIA’s analyzer.

#### LinkedIn / demo

Strong: “26% of skills vulnerable (author-cited research) — here’s a red/green scan before install.” Public thin harness + one-pager linking SkillSpector.

#### Technical compare

- **OpenTrustBench (today):** Trust Card/badge CI product with 8 rules; SkillSpector is **depth + NVIDIA pipeline**.
- **skillhawk / bastionsupply / mcplint (fresh small repos):** same problem space, far less pattern coverage / ecosystem weight.
- **v4scan (queued):** install-time MCP/skill scanner; SkillSpector is the **momentum default** with optional LLM stage.
- **SIGIL / SkillGuard papers (queued):** research on skill audit/runtime gaps; SkillSpector is the **shipping scanner**.
